spring:
  application:
    name: agrikarma-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/agrikarma?options=-c%20TimeZone=UTC
    username: agrikarma_user
    password: agrikarma_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC

  kafka:
    bootstrap-servers: localhost:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

    consumer:
      group-id: agrikarma-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

  mail:   #  for gmail smtp setup
    host: smtp.gmail.com
    port: 587
    username: satyamchoubey95720@gmail.com
    password: oropogclcdbxubjq
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  port: 8080